const path = require("path");
require("dotenv/config");

module.exports = {
  config: path.resolve(
    __dirname,
    "src",
    "config",
    (process.env.DB_DIALECT = "mysql" ? "database_mysql.js" : "database.js")
  ),
  "migrations-path": path.resolve(__dirname, "src", "database", "migrations"),
};
